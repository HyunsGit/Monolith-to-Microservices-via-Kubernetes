apiVersion: v1
kind: Service
metadata:
  name: carflix-backend-service
  namespace: backend
  annotations:
    alb.ingress.kubernetes.io/healthcheck-path: /healthy
spec:
  selector:
    app: carflix-backend
  type: ClusterIP
  ports:
   -  protocol: TCP
      port: 443
      targetPort: 8081
